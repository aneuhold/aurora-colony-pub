/**
 * Categories the menu page is divided into. Mirrors the `category` enum on
 * the `menuImages` content collection — keep the two in sync.
 */
export type MenuCategory = 'main' | 'breakfast' | 'kids' | 'happy-hour';
